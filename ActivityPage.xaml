<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:Diploma_cs.Controls"
             x:Class="Diploma_cs.ActivityPage">
    <ScrollView>
        <VerticalStackLayout Style="{StaticResource VerticalStackProperties}">
            <Label Text="Aktywność" Style="{StaticResource Heading1}"/>
            <HorizontalStackLayout
                Padding="2, 0"
                Spacing="10">
                <ImageButton
                    x:Name="previousMonthButton"
                    Source="arrowleftactive.png"
                    Style="{StaticResource CalendarHeading2Button}"
                    Clicked="PreviousMonthClicked"/>
                <Label
                    x:Name="monthYearLabel"
                    Text="Czerwiec 2025"
                    Style="{StaticResource Heading2}"/>
                <ImageButton
                    x:Name="nextMonthButton"
                    Source="arrowrightactive.png"
                    Style="{StaticResource CalendarHeading2Button}"
                    Clicked="NextMonthClicked"/>
            </HorizontalStackLayout>
            <Grid
                x:Name="calendarGrid"
                ColumnDefinitions="*, *, *, *, *, *, *"
                RowDefinitions="*, *, *, *, *, *, *"
                ColumnSpacing="10"
                RowSpacing="10">
                
                <Label
                    Text="pon"
                    FontSize="12"
                    Grid.Column="0"
                    Grid.Row="0"
                    HorizontalOptions="Center"
                    VerticalOptions="Center"/>
                <Label
                    Text="wto"
                    FontSize="12"
                    Grid.Column="1"
                    Grid.Row="0"
                    HorizontalOptions="Center"
                    VerticalOptions="Center"/>
                <Label
                    Text="śro"
                    FontSize="12"
                    Grid.Column="2"
                    Grid.Row="0"
                    HorizontalOptions="Center"
                    VerticalOptions="Center"/>
                <Label
                    Text="czw"
                    FontSize="12"
                    Grid.Column="3"
                    Grid.Row="0"
                    HorizontalOptions="Center"
                    VerticalOptions="Center"/>
                <Label
                    Text="pią"
                    FontSize="12"
                    Grid.Column="4"
                    Grid.Row="0"
                    HorizontalOptions="Center"
                    VerticalOptions="Center"/>
                <Label
                    Text="sob"
                    FontSize="12"
                    Grid.Column="5"
                    Grid.Row="0"
                    HorizontalOptions="Center"
                    VerticalOptions="Center"/>
                <Label
                    Text="nie"
                    FontSize="12"
                    Grid.Column="6"
                    Grid.Row="0"
                    HorizontalOptions="Center"
                    VerticalOptions="Center"/>
            </Grid>

            <RoundRectangle Style="{StaticResource BlockSeparator}"/>
            
            <Label Text="Twoje tendencje" Style="{StaticResource Heading2}"/>
            
            <controls:Last7DaysStatsView x:Name="Last7DaysStats" />

            <GraphicsView
                x:Name="DashedSeparator"
                HeightRequest="5"
                HorizontalOptions="Fill"
                VerticalOptions="Center"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>